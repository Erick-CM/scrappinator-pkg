declare module '@scrappinator' {
    export interface Scrappinator {
      // Add properties and methods here based on Scrappinator class definition
    }

    export interface ScrapperLib {
      // Add properties and methods here based on ScrapperLib interface definition
    }

    export interface Goto {
        url: string;
        // Add other properties and methods of Goto interface
    }
}